#include <unist.h>

/**
 * _putchar - write the character c to stdout
 * The chracter to print
 *
 * Return: on success 1.
 * On error, -1 is returned, and errno is set apppropriately.
 */
int _putchar(char c)
{
  return (write(1, &c,1));
}
